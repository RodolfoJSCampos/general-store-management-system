import 'package:flutter/material.dart';

class FazerOrcamentoPage extends StatelessWidget {
  const FazerOrcamentoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Fazer Orçamento'),
      ),
      body: const Center(
        child: Text('Página de Fazer Orçamento'),
      ),
    );
  }
}

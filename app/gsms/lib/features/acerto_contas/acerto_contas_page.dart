import 'package:flutter/material.dart';

class AcertoContasPage extends StatelessWidget {
  const AcertoContasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Acerto de Contas'),
      ),
      body: const Center(
        child: Text('Página de Acerto de Contas'),
      ),
    );
  }
}
